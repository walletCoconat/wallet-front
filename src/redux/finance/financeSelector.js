export const getTotalBalance = state => state.finance.totalBalance
export const getFinanceData = state => state.finance.data

// export const getFilterDataTransaction = state => {
//     const allTransaction = getFinanceData(state)
//     //  дописать фильтрацию
// }
